/**
 * KONFIGURASI FRONTEND
 * ====================
 * Isi API_URL dengan URL Web App GAS setelah deploy.
 *
 * Cara mendapatkan URL:
 *   1. Buka project GAS Anda
 *   2. Klik "Deploy" → "Manage deployments" (atau "New deployment")
 *   3. Pilih tipe "Web app"
 *   4. Execute as: Me
 *   5. Who has access: Anyone (even anonymous)
 *   6. Klik Deploy, izinkan OAuth, salin URL yang muncul
 *   7. Tempel URL tersebut menggantikan nilai di bawah
 *
 * PERHATIAN:
 *   - URL ini boleh ada di file publik (tidak mengandung kredensial).
 *   - Jangan letakkan password atau kunci rahasia di file ini.
 */

const API_URL = 'Ganti';
